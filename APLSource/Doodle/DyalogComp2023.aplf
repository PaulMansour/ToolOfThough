 DyalogComp2023←{
     p1←{⍵/⍨⍵≥⌈\⍵}
     p2←⌽⍷⍥⌽
     p3←{a←' ',⎕A ⋄ a[27|⍺+a⍳⍵]}
     p4←{(⍺≢⍵)××-/⍋⍺ ⍵}      ⍝ APL Quest 2018-9 ≡???
     ⍝  (>-<)⍥(1 ⎕DT ⊂)
     ⍝  ⊃0~⍨×⍥-
     p5←{⌽(+/,+/∘~)⊃>/(⌊/≢¨⍺ ⍵)↑¨⍺ ⍵}
     p6←{{⍵⍴⍨2,⍨0.5×≢⍵}⍵⊆⍨~⍵∊⍺}
     p6←{f←≠⊆⊢ ⋄ ↑(0⊃⍺)f¨(1⊃⍺)f ⍵}
     P7←{1∧⍵,÷1∨⍵}
     p8←{3↑⊃¯1 ⎕DT s+21252 644 759 924{⍵+⌊/⍺|⍺-⍺|⍵}-/e s←1 ⎕DT ⍵ ⍺}
     p9←{⍉{(⊂⍋(⍳≢⍵)+(≢⍵)⍴1,¯1)⌷⍵}⍉⍵}
     p10←
 }

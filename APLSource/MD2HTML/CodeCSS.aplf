 CodeCSS←{
     s←⊂{s←⊂'code'
         s,←⊂'margin' '1rem'
         ⍝ s,←⊂'padding' '10rem'
         s,←⊂'font-family' 'APL385 Unicode'
         s,←⊂'font-size' '1.5em'
         ⍝ s,←⊂'font-weight' 'bold'
         s}⍵
     s,←⊂{s←⊂'pre'
         s,←⊂'padding' '.5rem '
         ⍝ s,←⊂'font-family' 'APL385 Unicode'
         ⍝ s,←⊂'font-size' '1.5em'
         ⍝ s,←⊂'font-weight' 'bold'
         s}⍵


     s
 }
